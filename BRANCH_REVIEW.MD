# BRANCH_REVIEW.MD

브랜치별 상태를 분리 검토한 결과 요약입니다.  
검토 기준 시점: 2026-02-17

## 1) Claude 브랜치 검토
- 기준 커밋: `97978fd`
- 성격: Claude(Anthropic) 기반 운영 스냅샷 보관

### 포함된 핵심 내용
- 로컬 전용 보안 정책 적용:
  - `BOT_SERVER_HOST=127.0.0.1`
  - Docker 포트 `127.0.0.1:8080:8080`
  - `BOT_API_TOKEN` 인증 적용
- 트레이딩봇 Telegram 명령 On/Off 제어 API 포함
- `/명령어` 한글 명령 별칭 포함
- 2026-02-16 아카이브/보안 정리 내용 반영

### 판단
- 장점:
  - 보안 기준이 이미 안정화된 스냅샷
  - Anthropic 기반 회귀/비교 기준점으로 적합
- 한계:
  - GPT 운영 정책 문서(`BRANCHING.MD`, GPT 연동 가이드)는 미포함

## 2) GPT 브랜치 검토
- 기준 커밋: `67e9b54`
- 성격: GPT(OpenAI) 기준 운영/확장 브랜치
- 현재 상태: `main`과 동일 커밋

### 포함된 핵심 내용
- `BRANCHING.MD` 추가 (main/GPT/Claude 운영 정책)
- `OPENCLAW.MD`에 OpenAI 키 등록 및 GPT 모델 전환 절차 문서화
- `README.md`에 GPT 우선 브랜치 전략 반영

### 판단
- 장점:
  - Telegram 중심 GPT 코딩 운영 절차가 명확함
  - 향후 `GPT`에서 실험 후 `main` 반영하는 운영 흐름 마련
- 한계:
  - 현재는 `main`과 완전히 동일해 기능 분리 이점이 아직 없음

## 3) 브랜치 간 실제 차이
- 코드(런타임/보안 로직) 차이: 없음
- 차이 파일:
  - `BRANCHING.MD` (GPT/main에만 존재)
  - `OPENCLAW.MD` (GPT 연동 섹션 추가)
  - `README.md` (브랜치 운영 전략 섹션 추가)

## 4) 불필요/수정 권고
1. 문서 중복 관리 리스크
- 현황: 브랜치 정책이 `README.md`, `OPENCLAW.MD`, `BRANCHING.MD`에 중복 기재
- 권고: 정책 원문은 `BRANCHING.MD`를 단일 기준으로 두고, 나머지는 링크/요약만 유지

2. GPT 브랜치 활용도
- 현황: `GPT == main` 상태
- 권고: 앞으로 GPT 관련 실험(모델/프롬프트/자동화) 변경은 `GPT` 브랜치에서 먼저 수행 후 `main`으로 반영

3. 모델 운용 주의
- 현황: OpenAI 연동은 성공했으나, 실제 응답은 계정 쿼터/레이트리밋 영향 가능
- 권고: 운영 전 OpenAI 사용량 한도/쿼터 상태를 점검하고, 실패 시 fallback 모델 정책 정의

## 5) 최종 결론
- Claude 브랜치: 보안 안정 스냅샷으로 유지 가치 높음
- GPT 브랜치: 운영 방향성과 문서 체계는 적절하며, 이후 실질적 변경은 GPT 브랜치에서 누적하는 방식이 타당함
